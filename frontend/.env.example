# Local backend
VITE_API_URL=http://localhost:8000

# Production / Vercel builds use .env.production (same-origin /api proxy).
# Or set VITE_API_URL=https://money-track-epnn.onrender.com in the Vercel project.
