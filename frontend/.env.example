# Dev: use /api so Vite proxies to uvicorn (avoids cross-origin / preview blocks).
VITE_API_URL=/api
# Or point straight at the API:
# VITE_API_URL=http://127.0.0.1:8000

# Analytics (optional — omit to disable)
# VITE_PLAUSIBLE_DOMAIN=tally.nuential.com
# VITE_POSTHOG_KEY=phc_xxx
# VITE_POSTHOG_HOST=https://us.i.posthog.com
