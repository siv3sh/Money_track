# Frontend (Vite + React + TypeScript + Tailwind)

```bash
cp .env.example .env
npm install
npm run dev
```

Set `VITE_API_URL` to your FastAPI base URL (default `http://localhost:8000`).

See the root [README](../README.md) for full setup (MongoDB, backend, iOS Shortcuts, deploy).
